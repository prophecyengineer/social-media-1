/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
    images: {
    domains: ["img.icons8.com", "unsplash.com", "placekitten.com/200/300"],
  },
};


module.exports = nextConfig;
